# `paws` connection factory

`paws` connection factory

## Usage

``` r
con_factory(service)
```

## Arguments

- service:

  (character) any service supported by paws

## Value

a function for returning the client for the service in the `service`
parameter
