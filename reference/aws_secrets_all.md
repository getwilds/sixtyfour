# Get all secret values

Get all secret values

## Usage

``` r
aws_secrets_all()
```

## Value

(tbl) with secrets

## Examples

``` r
if (FALSE) { # aws_has_creds() && interactive()
aws_secrets_all()
}
```
