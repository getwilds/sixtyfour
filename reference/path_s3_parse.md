# Parse s3 paths

Parse s3 paths

## Usage

``` r
path_s3_parse(paths)
```

## Arguments

- paths:

  (character) one or more s3 paths

## Value

an unnamed list with each slot a named list with bucket, path, and file
